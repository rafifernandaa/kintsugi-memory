steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:$COMMIT_SHA', '-t', 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:latest', '.']

  # 2. Push image to Google Container Registry / Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:$COMMIT_SHA']

  # 3. Deploy container image to Google Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'kintsugi-memory-service'
      - '--image'
      - 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:$COMMIT_SHA'
      - '--region'
      - 'us-west1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'GOOGLE_CLOUD_PROJECT=my-project-31-491314,GEMINI_MODEL=gemini-3.7-flash'

images:
  - 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/kintsugi-memory-agent:latest'

options:
  logging: CLOUD_LOGGING_ONLY
